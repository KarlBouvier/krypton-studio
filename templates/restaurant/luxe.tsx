/**
 * Optional template override for restaurant/luxe.
 * The main page uses shared sections + config; this file is here for future
 * sector/variant-specific layout or component overrides if needed.
 */
export {};
